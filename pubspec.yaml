name: doutang
description: Trouve ton chez-toi, seul ou à deux.
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.0.0
  share_plus: ^7.0.0
  file_picker: ^6.0.0
  path_provider: ^2.1.0
  http: ^1.0.0
  fl_chart: ^0.66.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
